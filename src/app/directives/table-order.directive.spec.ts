import { TableOrderDirective } from './table-order.directive';

describe('TableOrderDirective', () => {
  it('should create an instance', () => {
    const directive = new TableOrderDirective();
    expect(directive).toBeTruthy();
  });
});
