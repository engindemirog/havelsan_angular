import { GlobalizationPipe } from './globalization.pipe';

describe('GlobalizationPipe', () => {
  it('create an instance', () => {
    const pipe = new GlobalizationPipe();
    expect(pipe).toBeTruthy();
  });
});
